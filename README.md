# bootstrap-datepicker

A simple bootstrap datepicker with optional time selector. jQuery supported but not required

## Usage

* Datepicker

```
new Datepicker(input);
```

* With timepicker

```
new Datepicker(input, { showTime: true, showButtons: true  });
```

* With jQuery (optional)

```
$(input).datepicker();
```
